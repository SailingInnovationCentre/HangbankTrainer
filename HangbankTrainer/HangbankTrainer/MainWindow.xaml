<Window x:Class="HangbankTrainer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        xmlns:local="clr-namespace:HangbankTrainer"
        mc:Ignorable="d"
        Title="Hangbank Trainer" Height="700" Width="1300" 
        Closed="Window_Closed">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="DefaultResourceDictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <StackPanel>
        <TabControl HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="8">
            <TabItem Header="Configuratie"  HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <local:ConfigurationUserControl x:Name="ConfigurationUserControl"/>
            </TabItem>
            <TabItem Header="Training">
                <local:TrainingUserControl x:Name="TrainingUserControl"/>
            </TabItem>
        </TabControl>
    </StackPanel>
</Window>
